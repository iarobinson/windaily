<nav class="navbar fixed-bottom navbar-dark bg-dark text-white">
  <div class="col text-center">
    <%= link_to material_icon.settings, edit_user_path(current_user) %>
  </div>
  <div class="col text-center">
    <%= link_to material_icon.home, root_url %>
  </div>
  <div class="col text-center">
    <%= link_to material_icon.group, community_path %>
  </div>
  <div class="col text-center">
    <%= link_to material_icon.add_task, add_path %>
  </div>
</nav>
