<div class="<%= standard_column_setting %> mb-5 py-3">
  <%= render "challenges/wins/win", win: @win %>
  <% if current_user %>
    <%= link_to 'Edit', edit_challenge_win_path(@challenge, @win), class: "btn btn-block btn-primary" if @win.user == current_user %>
  <% end %>
  <%= link_to 'View Challenge', challenge_path(@challenge), class: "btn btn-block btn-primary" %>
</div>
