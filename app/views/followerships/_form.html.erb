<%= form_with(model: followership, local: true) do |form| %>
  <% if followership.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(followership.errors.count, "error") %> prohibited this followership from being saved:</h2>

      <ul>
        <% followership.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :friend_id %>
    <%= form.number_field :friend_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
