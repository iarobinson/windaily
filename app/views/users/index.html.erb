<div class="container">
  <div class="row">
    <div class="col-md-6 offset-md-3">
      <ul class="list-group">
        <% @users.each do |user| %>
          <%= link_to user.inspect, users_path(user), class: "list-group-item list-group-item-action" %>
        <% end %>
      </ul>
    </div>
  </div>
</div>
