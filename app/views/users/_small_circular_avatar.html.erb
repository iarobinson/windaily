<% if current_user.avatar.attached? %>
  <%= image_tag url_for(current_user.avatar), class: "rounded-circle m-3", style: "width:75px;max-height:75px;" %>
<% else %>
  <%= image_tag "default_avatar", class: "rounded-circle m-3", style: "width:75px;max-height:75px;" %>
<% end %>
